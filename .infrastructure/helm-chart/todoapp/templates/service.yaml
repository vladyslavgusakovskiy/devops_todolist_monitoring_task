apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: {{ .Values.common.namespace }}
  labels:
    job: todoapp-job
    app: todoapp
spec:
  type: ClusterIP
  selector:
    app: todoapp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080